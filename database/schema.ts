import { optionTable } from "@schema/option";
import { userTable } from "@schema/user";
import { productTable } from "@schema/product";
import { orderTable } from "@schema/order";

export const schema = {
  optionTable,
  userTable,
  productTable,
  orderTable
}
